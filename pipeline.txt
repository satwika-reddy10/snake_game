✅ 1. Jenkins Job Creation – Step-by-Step
Step 1: Create a New Item → Pipeline

Name:

MavenJava_ScriptedPipeline

Step 2: General

Fill in Description:

This Jenkins job demonstrates a scripted pipeline that clones a Maven project from GitHub and performs clean, install, test, and package stages.

✅ 2. Build Triggers

Choose any trigger your faculty told.

Examples:

Build periodically: H/2 * * * *

Poll SCM: H/5 * * * *

GitHub hook trigger: check the box

Build after other projects

✔ You can select Poll SCM for viva:

H/5 * * * *

✅ 3. Pipeline Definition

Go to:

Pipeline → Definition → Pipeline script

Now paste the corrected pipeline script below:

🟩 FINAL PIPELINE SCRIPT (WORKS 100%)

Important — replace the GitHub URL + project folder name.

pipeline {
    agent any
    tools{
        maven 'MAVEN_HOME'
    }
    stages {
        stage('git repo & clean') {
            steps {
                bat "rmdir  /s /q octYogitaJava || exit 0"
                bat "git clone https://github.com/anujyog1/octYogitaJava.git"
                bat "mvn clean -f octYogitaJava"
            }
        }
        stage('install') {
            steps {
                bat "mvn install -f octYogitaJava"
            }
        }
        stage('test') {
            steps {
                bat "mvn test -f octYogitaJava"
            }
        }
        stage('package') {
            steps {
                bat "mvn package -f octYogitaJava"
            }
        }
    }
}